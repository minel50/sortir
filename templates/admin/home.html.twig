{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Espace administrateur
{% endblock %}

{% block body %}
    <h2>Bienvenue sur l'espace administrateur</h2>
    <p>
        Voici les fonctionnalités réservées aux administrateurs :
    </p>
    <p>
        <a class="btn btn-outline-dark" href="{{ path('admin_createParticipant') }}">Créer un participant</a>
    </p>
    <p>
        <a class="btn btn-outline-dark" href="{{ path('participant_all') }}">Modifier/Supprimer un participant</a>
    </p>
    <p>
        <a class="btn btn-outline-dark" href="{{ path('admin_sortie_list') }}">Annuler une sortie</a>
    </p>
{% endblock %}

